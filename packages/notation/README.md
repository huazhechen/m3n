# @m3n/notation

M3N 的纯 TypeScript 领域包，提供语法树、规范化乐谱模型、诊断、格式化、复杂度分析和 MEI 转换。

```ts
import { analyzeM3N, formatM3N, m3nToMei } from '@m3n/notation'
```

该包不依赖 React、DOM 或任何播放器，可在浏览器构建工具和服务端工具中复用。

```sh
npm run build -w @m3n/notation
npm run test -w @m3n/notation
```
