# M3N 手册

> [返回简介](README.md)

本手册用于查询 M3N 的完整语法、约束和推荐写法。它不是学习路线；第一次写谱请先阅读 [M3N 指南](GUIDE.md)。

M3N 的核心设计是：正文先写完整音乐，歌词、低音谱表等补充内容再作为独立片段追加在正文之后。小节是音乐组织的核心单元，因此圆括号、音符分组等会改变小节内部节奏结构的语法，都必须在同一小节内闭合。

## 1. 文档结构与属性

### 1.1 文件、空白与注释

- M3N 是纯文本格式。
- 空格、Tab、换行都可以作为分隔符。
- 连续空白不会改变音乐语义，但建议用空格和换行表现乐句结构。
- `//` 表示行内注释，从 `//` 到行尾的内容不参与解析。
- M3N 不支持块注释。

### 1.2 文档组成

一份完整 M3N 文档通常由三段组成：

1. 元信息：标题、作者、来源等描述性信息。
2. 音乐正文：从调号、拍号、速度等初始状态开始，继续写音符、小节线、反复和结构标记。
3. 补充片段：正文结束后追加的歌词、低音谱表等独立内容。

```m3n
// 元信息
{title=标题}

// 音乐正文：调号、拍号、速度是正文的起始状态
{key=C} {4/4} {tempo=100bpm}
1 2 3 4 | 5 6 7 1e |||

// 补充片段：写在正文结束之后
{lyrics}
词 一 词 二 词 三 词 四
{/}

{bass}
1d 5d 1 5d | 1d^^ |||
{/}
```

规则：

- 元信息描述乐谱，不直接决定音符如何解释。
- 音乐正文是文档的主体，包含调号、拍号、速度、音符、小节线、反复和结构标记。
- 歌词块、低音谱表块写在音乐正文完成之后，作为额外补充片段。
- `|||` 表示音乐正文的终止线；若正文结尾同时需要后反复，使用 `:|||`。
- 补充片段不改变正文的音符顺序，只为正文添加歌词、附加谱表等信息。

### 1.3 属性形式

M3N 的属性统一使用大括号：

```m3n
{key=value}
{flag}
{/}
{/name}
```

属性不需要只按解析行为记忆。使用时更自然的方式，是按它们服务的目标查找：

| 用途 | 例子 | 说明 |
| :--- | :--- | :--- |
| 乐谱信息 | `{title=...}`、`{composer=...}` | 描述标题、作者、来源等信息 |
| 乐曲设置 | `{key=C}`、`{4/4}`、`{tempo=120bpm}` | 决定后续音乐如何解释和播放 |
| 排版与段落 | `{br}`、`{part=A}`、`{parts=A-B-A}` | 影响换行、段落标记和段落播放顺序 |
| 谱面标注 | `{text=rit.}`、`{chord=Am}`、`{mf}` | 在谱面上添加文字、和弦、力度等标记 |
| 演奏与区间 | `{tr}`、`{lg} ... {/lg}`、`{8va} ... {/8va}` | 标记奏法，或作用于一段连续内容 |
| 结构跳转 | `{segno}`、`{fine}`、`{DC}`、`{DS}` | 表示反复和跳转结构 |
| 补充片段 | `{lyrics} ... {/}`、`{bass} ... {/}` | 写在音乐正文结束后的独立内容 |

其中有些属性只作用于当前位置，有些会持续影响后续内容，有些必须显式闭合。具体规则按用途分别说明。

### 1.4 乐谱信息

| 写法 | 含义 |
| :--- | :--- |
| `{title=标题}` | 标题 |
| `{subtitle=副标题}` | 副标题 |
| `{composer=...}` | 作曲者 |
| `{lyricist=...}` | 作词者 |
| `{arranger=...}` | 编曲者 |
| `{copyright=...}` | 版权信息 |
| `{source=...}` | 来源说明 |
| `{note=...}` | 备注 |
| `{meta=...}` | 自定义元信息 |

同一字段可以重复声明，例如多位作曲者。渲染器应按声明顺序合并或展示。

### 1.5 乐曲设置、排版与谱面标注

| 写法 | 含义 | 规则 |
| :--- | :--- | :--- |
| `{key=C}` | C 大调 | 推荐调号写法 |
| `{key=Am}` | A 小调 | `m` 表示自然小调 |
| `{key=Ddor}` | D 多利亚 | 支持教会调式后缀 |
| `{1=C}` | C 大调 | 历史写法，等价于 `{key=C}` |
| `{4/4}` | 拍号 | 分母决定普通音符的基础时值 |
| `{tempo=120bpm}` | 速度 | 影响播放和谱面速度标记 |
| `{part=A}` | 段落标记 | 标出正文中的段落位置 |
| `{parts=A-B-A}` | 段落播放顺序 | 影响播放器按段落播放的行为 |
| `{br}` | 换行 | 渲染提示，不改变音乐 |
| `{rest=8}` | 多小节休止 | 独立占据指定小节数 |
| `{text=rit.}` | 谱面文字 | 附着在当前位置或前一个元素附近 |
| `{chord=Am}` | 和弦标记 | 作用到本小节内下一个和弦标记或小节末尾 |

持续性规则：

- `{key=...}`、拍号、`{tempo=...}` 从声明位置开始生效，通常持续到同类设置再次出现，或文档结束。
- `{br}` 只作用于当前位置，是渲染换行提示。
- `{part=...}` 标出正文中的段落位置。
- `{parts=...}` 描述段落播放顺序，并会影响播放器按段落展开音乐。
- `{text=...}` 只添加谱面文字，不改变音乐语义。
- `{chord=...}` 从声明位置生效，持续到本小节内的下一个和弦标记，或当前小节末尾。

## 2. 音符、节奏与小节

### 2.1 音符单元

一个普通音符的完整形式是：

```text
<音级><临时变音><八度修饰><时值后缀><后置属性>
```

只有音级必填，其余部分可选。例子：

```m3n
1
4#
7bd
1e^.
5{tr}
```

### 2.2 音高

| 元素 | 写法 | 说明 |
| :--- | :--- | :--- |
| 音级 | `1 2 3 4 5 6 7` | 当前调式下的七个音级 |
| 升号 | `#` | 升高半音，可叠加，如 `4##` |
| 降号 | `b` | 降低半音，可叠加，如 `7bb` |
| 还原号 | `=` | 还原同小节内前面的临时变音 |
| 高八度 | `e` | 来自 `elevated`，可叠加，如 `1ee` |
| 低八度 | `d` | 来自 `dropped`，可叠加，如 `6dd` |
| 休止符 | `0` | 无音高，但参与时值计算 |

规则：

- 顺序必须是音级、临时变音、八度修饰、时值后缀。
- 临时变音在同一小节内持续生效，到小节线处失效。
- 八度修饰只作用于当前音符，不向后传播。
- `0` 可以使用时值后缀 `^` 和 `.`，但不能使用延音 `~`，也不能附加音高相关属性。

### 2.3 调号与调式

`{key=...}` 决定 `1 2 3 4 5 6 7` 的默认音高关系。

| 写法 | 含义 |
| :--- | :--- |
| `{key=C}` | C 大调 |
| `{key=Am}` | A 自然小调 |
| `{key=Ddor}` | D 多利亚 |
| `{key=Ephr}` | E 弗里几亚 |
| `{key=Flyd}` | F 利底亚 |
| `{key=Gmix}` | G 混合利底亚 |
| `{key=Bloc}` | B 洛克里亚 |

优先选择符合乐曲调式的 `{key=...}`，不要用大量临时升降号硬写另一个调式。

### 2.4 时值

拍号分母决定普通音符的基础时值。例如在 `{4/4}` 中，不带括号和后缀的 `1` 是四分音符。

| 写法 | 含义 |
| :--- | :--- |
| `1` | 基础时值 |
| `(1)` | 当前括号层级内时值减半 |
| `((1))` | 减半两次 |
| `1^` | 时值翻倍 |
| `1^^` | 再翻倍 |
| `1.` | 附点，增加当前时值的一半 |
| `1^.` | 先翻倍，再附点 |
| `1~ 1` | 延音，连接后方同绝对音高音符 |

规则：

- 每嵌套一层圆括号，括号内音符时值减半。
- 圆括号必须在同一小节内闭合。
- `^` 和 `.` 可以叠加；同时使用时必须先 `^` 后 `.`。
- `~` 可以跨小节，也可以连续形成延音链。
- `~` 连接的目标必须是后方第一个绝对音高完全相同的音符。

### 2.5 小节线与反复小节线

| 写法 | 含义 |
| :--- | :--- |
| `|` | 普通小节线 |
| `|||` | 终止线 |
| `||:` | 前反复 |
| `:||` | 后反复 |
| `:|||` | 后反复与终止线合并，等价于 `:||` 后直接结束 |
| `:||:` | 后反复结束后立即进入新的前反复 |

约束：

- 圆括号 `()` 必须在同一小节内闭合。
- 音符分组 `[...]` 必须完整位于同一小节内。
- 区间属性可以跨小节，但必须显式闭合。
- 开头小节不满拍时，渲染器可自动推断弱起；M3N 不提供专门的弱起声明。

### 2.6 音符分组

音符分组使用中括号，组内只写音高词法单元：

```text
[notes:mode]
```

组内不允许出现休止符、时值后缀、后置属性、圆括号或嵌套分组。

| 写法 | 含义 |
| :--- | :--- |
| `[1 2 3:2]` | 连音组，三个音均分两份当前位置时值 |
| `[1 3 5:h]` | 和声组，三个音同时发声 |

分组作为一个整体占据当前位置的一个时值单元，并且必须完整位于同一小节内。

### 2.7 多小节休止

```m3n
{rest=4}
```

- `N` 必须是正整数。
- `{rest=N}` 独立占据 `N` 个小节。
- 多小节休止不与普通音符混写在同一小节中。

## 3. 标记、演奏法与结构

### 3.1 后置属性

后置属性附着在前一个音符之后：

```m3n
1{tr}
1{ac(2)}
1{ap(2)}
1{ac+((2 3))}
```

后置属性只作用于它前面的音符或记谱位置，不会向后传播。

### 3.2 装饰音

| 写法 | 名称 | 说明 |
| :--- | :--- | :--- |
| `{ac(...)}` | 前短倚音 | `ac` 来自 `acciaccatura`，通常渲染为带斜线小音符，主音时值不变 |
| `{ac+(...)}` | 后短倚音 | `+` 表示位于主音之后 |
| `{ap(...)}` | 前长倚音 | `ap` 来自 `appoggiatura`，通常渲染为不带斜线小音符，并占用主音时值 |
| `{ap+(...)}` | 后长倚音 | `+` 表示位于主音之后 |

规则：

- 倚音内容按“单音符或音符组”解析。
- 短倚音不改变主音时值。
- 长倚音默认占用主音时值的一部分；渲染器可按常规记谱习惯处理，也可读取内部显式时值。
- 每个主音至多附着一个装饰音属性。

### 3.3 奏法记号

| 写法 | 含义 |
| :--- | :--- |
| `{tr}` | 颤音 |
| `{wav}` / `{wav+}` | 上波音 |
| `{wav-}` | 下波音 |
| `{echo}` | 回音 |
| `{str}` | 重音 |
| `{brk}` | 顿音 |
| `{tip}` | 跳音 |
| `{hold}` | 保持音 |
| `{breath}` | 呼吸记号 |

规则：

- 每个主音至多附着一个普通奏法记号。
- `{breath}` 可与其他奏法记号并存。
- `{breath}` 可以附着于音符后，也可以附着于小节线后。
- 装饰音属性和奏法记号可以并存。

### 3.4 区间属性与即时力度

区间属性作用于一段连续内容，必须显式闭合：

```m3n
{lg}1 2 3 4{/}
{cresc}1 2 | 3 4{/cresc}
```

| 写法 | 含义 |
| :--- | :--- |
| `{lg}` | 连奏起点 |
| `{cresc}` | 渐强起点 |
| `{decres}` | 渐弱起点 |
| `{8va}` | 高八度区间起点 |
| `{8vb}` | 低八度区间起点 |
| `{volta=1}` | 第 1 房子起点 |
| `{volta=2}` | 第 2 房子起点 |

闭合写法：

- `{/}`：关闭最近一个尚未闭合的区间属性。
- `{/lg}`、`{/cresc}`、`{/volta}`：关闭指定类型，推荐在复杂段落中使用。

力度记号是即时属性，从当前位置开始生效，直到被新的力度覆盖：

```m3n
{p}1 2 3 4 | {mf}5 6 7 1e | {f}1e^^ |||
```

支持的力度记号：

`ppp`、`pp`、`p`、`mp`、`mf`、`f`、`ff`、`fff`、`fp`、`sfz`

### 3.5 反复与跳转结构

小节线反复：

```m3n
||: 1 2 3 4 | 5 6 7 1e :|||
```

跳房子：

```m3n
||: 1 2 3 4 | {volta=1}5^ 0^{/volta}:||
{volta=2}1^^{/volta}|||
```

结构跳转：

| 写法 | 含义 |
| :--- | :--- |
| `{segno}` | Segno 标记 |
| `{segno=A}` | 具名 Segno |
| `{fine}` | Fine 终点 |
| `{DC}` | Da Capo，回到开头 |
| `{DS}` | Dal Segno，回到最近的 Segno |
| `{DS=A}` | 回到指定名称的 Segno |

结构属性本身不形成区间，不需要闭合。播放展开细节按常规西方记谱习惯和渲染器实现处理。

## 4. 补充片段

### 4.1 歌词块

歌词块写在所有音乐正文结束之后：

```m3n
{key=C} {4/4}
1 1 5 5 | 6 6 5^ |||

{lyrics}
一 闪 一 闪 亮 晶 晶
{/}
```

遍次范围：

```m3n
{lyrics=1}
第 一 遍 歌 词
{/}

{lyrics=2,4~6}
第 二 四 到 六 遍 歌 词
{/}
```

歌词内容规则：

| 元素 | 说明 |
| :--- | :--- |
| 空格分隔的词语 | 每个词语对应一个有音高的音符 |
| `%` | 占位符，表示该音符无歌词，但仍消耗一个歌词位置 |
| `-` | 连字符，用于将英文单词拆分到多个音符 |

补充规则：

- 休止符 `0` 不占用歌词位置。
- 连字符拆分出的各音节分别对应连续音符。
- 多个汉字不含空格时，视为整体附着到同一个音符。
- 歌词对应关系以实际发声的独立音符为准；附点和延音不额外产生歌词位置。

### 4.2 低音谱表块

低音谱表块也写在音乐正文结束之后，作为独立补充片段：

```m3n
{title=双谱表示例}
{key=C} {4/4}
1 2 3 4 | 5 6 7 1e |||

{bass}
[1d 5d:h] [1d 5d:h] [1d 5d:h] [1d 5d:h] |
[1d 5d:h] [1d 5d:h] 0^ |||
{/}
```

低音谱表内部使用与正文相同的音高、时值、小节线和分组语法。它应只包含谱表级音乐内容，不再嵌入歌词块等文档级补充片段。

## 5. 语法速查与推荐写法

### 5.1 语法速查

| 目标 | 写法 |
| :--- | :--- |
| 标题 | `{title=标题}` |
| 作曲者 | `{composer=...}` |
| 调号 | `{key=C}`、`{key=Am}`、`{key=Ddor}` |
| 拍号 | `{4/4}` |
| 速度 | `{tempo=120bpm}` |
| 段落 | `{part=A}` |
| 段落播放顺序 | `{parts=A-B-A}` |
| 强制换行 | `{br}` |
| 多小节休止 | `{rest=8}` |
| 谱面文字 | `{text=rit.}` |
| 和弦标记 | `{chord=Am}` |
| 普通音符 | `5` |
| 升音 / 降音 / 还原 | `4#`、`7b`、`4=` |
| 高八度 / 低八度 | `1e`、`5d` |
| 休止符 | `0` |
| 八分音符 | `(5)` |
| 二分音符 | `5^` |
| 附点音符 | `5.` |
| 延音 | `5~ 5` |
| 三连音 | `[1 2 3:2]` |
| 和声组 | `[1 3 5:h]` |
| 前短倚音 / 后短倚音 | `5{ac(6)}`、`5{ac+(6)}` |
| 前长倚音 / 后长倚音 | `5{ap(6)}`、`5{ap+(6)}` |
| 颤音 | `5{tr}` |
| 上波音 / 下波音 | `5{wav}`、`5{wav-}` |
| 连奏区间 | `{lg}1 2 3{/lg}` |
| 渐强区间 | `{cresc}1 2 3{/cresc}` |
| 前反复 / 后反复 | `||:`、`:||` |
| 后反复并终止 | `:|||` |
| 跳房子 | `{volta=1} ... {/volta}` |
| 歌词块 | `{lyrics} ... {/}` |
| 低音谱表块 | `{bass} ... {/}` |

### 5.2 推荐写法

- 文档开头先写标题等元信息；音乐正文开头显式声明调号、拍号和速度。
- 音乐正文先完整写完，再追加歌词和低音谱表。
- 每行尽量对应一个乐句或一个清晰片段。
- 小节线前后保留空格，提升可读性。
- 复杂区间优先使用 `{/lg}`、`{/cresc}`、`{/volta}` 这类具名闭合。
- 对跨段落的大结构使用 `{part=...}` 与 `{parts=...}`；对局部重复使用小节线反复。
- 对复杂节奏优先用圆括号表达层级，不依赖读者心算。
- 如果一个反复同时结束整首乐曲，统一写作 `:|||`，不要写成 `:|| |||`。
