# M3N 手册

> [返回简介](README.md)

本手册是 M3N 的完整语法参考，面向已掌握基本用法的用户。首次使用请先阅读 [M3N 指南](GUIDE.md)。

---

### 1. 文档结构

#### 1.1 文件格式

- 纯文本格式，扩展名 `.m3n`
- 空格、Tab、换行在语法解析时无含义
- `//` 从该符号到行尾均为注释，不参与解析

#### 1.2 文档组成

```
乐谱信息（可选）
乐谱正文（调号 → 拍号 → 速度 → 音符 → 小节线 → 终止线）
补充片段（可选，歌词块、低音谱表块）
```

示例：

```m3n
// 乐谱信息
{title=标题} {composer=作曲者}

// 乐谱正文
{key=Cmaj} {4/4} {120qpm}
1 2 3 4 | 5 6 7 1e |||

// 补充片段
{lyrics}
do re mi fa sol la si do
{/}

{bass}
1d 5d 1 5d | 1d^^ |||
{/}
```

#### 1.3 属性形式

属性统一使用大括号，主要包括以下模式：

| 模式名 | 格式规则 | 示例 |
| :--- | :--- | :--- |
| Key-Value | `{$key=$value}` | `{title=欢乐颂}` |
| Flag | `{$flag}` | `{4/4}` |
| Self-Closing | `{/}` | `{/}` |
| Named-Closing | `{/$name}` | `{/volta}` |

---

### 2. 乐谱信息

| 属性 | 含义 |
| :--- | :--- |
| `{title=...}` | 标题 |
| `{subtitle=...}` | 副标题 |
| `{composer=...}` | 作曲者 |
| `{lyricist=...}` | 作词者 |
| `{arranger=...}` | 编曲者 |
| `{copyright=...}` | 版权 |
| `{source=...}` | 来源 |
| `{note=...}` | 备注 |
| `{transpose=<+/->半音数}` | 移调 |

同一字段禁止重复声明。

---

### 3. 乐曲设置

乐曲设置从声明位置生效，持续到同类设置再次出现或文档结束。

#### 3.1 调号

调号由主音和调式组成：`{key=$Tonic[$Mode]}`

调式后缀：

| 后缀 | 调式 | 示例 |
| :--- | :--- | :--- |
| 无 | 自然大调 | `{key=C}`|
| `dor` | 多利亚 | `{key=Ddor}` |
| `phr` | 弗里几亚 | `{key=Ephr}` |
| `lyd` | 利底亚 | `{key=Flyd}` |
| `mix` | 混合利底亚 | `{key=Gmix}` |
| `m` | 自然小调 | `{key=Am}` |
| `loc` | 洛克里亚 | `{key=Bloc}` |


```m3n
// 不同主音
{key=Bb} (1d2d3d4d5d6d7d1) | {key=F} (12345671e) | {key=C} (12345671e) | {key=G} (12345671e) | {key=D} (12345671e) |
// 不同调式
{key=Dm} (12345671e) | {key=Dmix} (12345671e) |||
```

#### 3.2 拍号

拍号语法：`{每小节中单位拍的数目/单位拍的音符时值}`

分子必须为正整数；分母必须是 2 的正整数次幂，以对应可表示的音符时值，例如 `1`、`2`、`4`、`8`、`16`。因此 `{3/3}` 非法，因为不存在三分音符。

```m3n
// 常见拍号
{4/4} 5 5 5 5 | {2/4} 5 5 | {2/2} 5 5 5 5 |
{3/4} 5 5 5 | {6/8} (5 5 5) (5 5 5) |||
```

#### 3.3 速度

速度语法：`{<N>qpm}`，其中 `qpm` 是指每分钟多少个四分音符(Quarter Note Per Minute)。

```m3n
// 速度变化
{120qpm} (1234)(5671e) |
{60qpm} (1234)(5671e) |||
```
---

### 4. 音符

#### 4.1 音符结构

完整形式：`<音级>[临时变音...][八度修饰...][时值后缀...]`

#### 4.2 音高

| 元素 | 写法 | 说明 |
| :--- | :--- | :--- |
| 音级 | `1 2 3 4 5 6 7` | 首调音级 |
| 升号 | `#` | 升高半音，可叠加 `4##` |
| 降号 | `b` | 降低半音，可叠加 `7bb` |
| 还原号 | `=` | 还原同小节前面的临时变音 |
| 高八度 | `e` | elevate，升高一个八度，可叠加 |
| 低八度 | `d` | descend，降低一个八度，可叠加 |
| 休止符 | `0` | 无音高，参与时值计算 |
| 高八度区间 | `{8va}`...`{/}` | 区间内所有音提高八度 |
| 低八度区间 | `{8vb}`...`{/}` | 区间内所有音降低八度 |

规则：
- 临时变音持续生效到小节结束，直到遇到同音的其他变音
- 八度修饰只作用于当前音符

示例：
```m3n
1 2 3 4 |
// 临时变音持续生效
1# 2b 1 2 | 
// 临时变音叠加还原
1## 2bb 1= 2= |
// 八度修饰
1dd 1d 1e 1ee |
// 八度区间
{8vb}3 2 1^{/}|||
```

#### 4.3 时值

每个音符默认为四分音符，通过括号系统和时值后缀可以修改其时值。

| 写法 | 含义 | 以四分音符为基准 |
| :--- | :--- | :--- |
| `1` | 四分音符 | 1 倍 |
| `(1)` | 括号内时值减半 | 1/2 倍 |
| `((1))` | 减半两次 | 1/4 倍 |
| `(((1)))` | 减半三次 | 1/8 倍 |
| `1^` | 时值翻倍 | 2 倍 |
| `1^^` | 翻倍两次 | 4 倍 |
| `1.` | 附点 | 1.5 倍 |
| `1..` | 双附点 | 1.75 倍 |
| `1^.` | 先翻倍后附点 | 3 倍 |
| `5~ 5` | 延音 | 相加 |

规则：
- 三种时值后缀的书写顺序为：`[^][.][~]`
- 括号须在同一小节内闭合
- `~` 可跨小节，`~` 后方紧跟的第一个音符绝对音高必须和本音符相同

示例：
```m3n
// 括号/翻倍
1 (1(1 1)) 1^ |
// 附点
1. (1) 1..((1)) |
// 延音
(1 2~) 2~ (2 3) 1|
// 组合
1^.~ (1 1)|||
```

#### 4.4 音符分组

分组语法：`[notes:mode]`，组内只写音高词法单元。

| 写法 | 含义 | 说明 |
| :--- | :--- | :--- |
| `[1 3 5:h]` | 和声组 | 音符同时发声，占 1 份时值 |
| `[1 2 3:2]` | 连音组 | 3 音均分 2 份时值 |

规则：分组必须完整位于同一小节内；组内不允许休止符、时值后缀、后置属性、圆括号或嵌套分组。
示例
```m3n
{3/4}
[1 3 5:h] [5 7 2e:h] [1 3 5:h] |
([1 3 5:2]) ([5 7 2e:2]) ([1 3 5:2]) |||
```

---

### 5. 小节线

- `|` → 普通小节线 
- `||` → 双小节线（段落线）
- `|||` → 终止线
- `||:` → 前反复起点
- `:||` → 后反复终点
- `:|||` → 后反复与终止线合并
- `:||:` → 后反复结束后立即进入新前反复

---

### 6. 演奏标记

#### 6.1 力度记号

即时属性，从当前位置生效直到被新的力度覆盖。

| 写法 | 含义 | 写法 | 含义 |
| :--- | :--- | :--- | :--- |
| `{cresc}`...`{/}` | 渐强区间 | `{decres}`...`{/}` | 渐弱区间 |
| `{ppp}` | 极弱 | `{fff}` | 极强 |
| `{pp}` | 很弱 | `{ff}` | 很强 |
| `{p}` | 弱 | `{f}` | 强 |
| `{mp}` | 中弱 | `{mf}` | 中强 |
| `{sfz}` | 特强 | | |

示例：
```m3n
{cresc}1 2 3 4 {/}|{decres}5 4 3 2 {/}|
{pp}1 2 3 4 | {p}5 4 3 2 |
{mp}1 2 3 4 | {mf}5 4 3 2 |
{f}1 2 3 4 | {ff}5 4 3 2 |
| 1 5 {sfz}1^ |||
```

#### 6.2 奏法记号

| 写法 | 含义 | 写法 | 含义 |
| :--- | :--- | :--- | :--- |
| `{lg}`...`{/}` | 连奏区间 |||
| `{tr}` | 颤音 | `{echo}` | 回音 |
| `{str}` | 重音 | `{hold}` | 保持音 |
| `{brk}` | 顿音 | `{breath}` | 呼吸记号 |
| `{tip}` | 跳音 | `{f1}`~`{f5}` | 指法 |
| `{wav}` | 上波音 | `{wav-}` | 下波音 |

示例：
```m3n
{lg}1 2 3 4 {/}|
5{tr} 5{str} 5{brk} 5{tip} |
5{echo} 5{hold} 5{breath} 5{wav} |
5{wav-} 5{f1} 5{f3} 5{f5} |||
```
#### 6.3 装饰音

| 写法 | 名称 | 说明 |
| :--- | :--- | :--- |
| `{ac(...)}` | 短倚音 | acciaccatura，带斜线小音符，主音时值不变 |
| `{ap(...)}` | 长倚音 | appoggiatura，不带斜线，占用主音时值 |

示例：
```m3n
{key=C} {4/4}
3{ac(2)} 4 5{ac(34)} 3 |
5{ap(6)} 4 3{ap(2)} 1 |||
```

#### 6.4 和弦

语法规则为 `{chord=<C>}`，和弦持续到下一和弦标记出现。使用罗马数字级数标记，与首调体系一致：
| 写法 | 含义 | C 调示例 |
| :--- | :--- | :--- |
| I | 大三和弦 | C |
| ii | 小三和弦 | Dm |
| iii | 小三和弦 | Em |
| IV | 大三和弦 | F |
| V | 大三和弦 | G |
| vi | 小三和弦 | Am |
| vii° | 减三和弦 | Bdim |

大写罗马数字 = 大三和弦，小写 = 小三和弦。非自然音和弦通过后缀标注，如 V7（属七）、VIm（大六级）等。

---

### 7. 反复

#### 7.1 小节线反复
语法为 `||: ... :||`，意思是演奏到后反复线，从前反复线开始再演奏一次。
如果只有后反复线，通常表示从乐曲开头再演奏一次。
示例：
```m3n
1 2 3 4 :|| 5 4 3 2 ||: 1 2 3 4 :||: 5 3 1^ :|||
```
#### 7.2 跳房子

| 写法 | 含义 |
| :--- | :--- |
| `{volta=N}` | 第 N 房子 |
| `{volta=N,M}` | 第 N、M 房子 |
| `{volta=N~M}` | 第 N 到 M 房子 ||
| `{volta=X,N~M}` | 第X房子、第 N 到 M 房子 ||

示例：

```m3n
3 3 4 5 | 5 4 3 2 | 1 1 2 3 |{volta=1} 3. (2) 2^ {/}:||
{volta=2} 2. (1) 1^ {/}|||
```

#### 7.3 段落标记

使用`{part}...{/}`给乐段命名，然后通过`{parts=A B A C}`指定演奏顺序。

示例：
```m3n
{key=D} {4/4}
{parts=A B A C D C}

{part=A}
3 3 4 5 | 5 4 3 2 ||
{/}

{part=B}
1 1 2 3 | 3. (2) 2^ ||
{/}

{part=C}
1 1 2 3 | 2. (1) 1^ ||
{/}

{part=D}
2 2 3 1 | 2 (3 4) 3 1 | 2 (3 4) 3 2 | 1 2 5d 3~ | 3 3 4 5 | 5 4 3 2 ||
{/}
```

---

### 8. 排版与标注

- 使用 `{br}` 建议渲染器换行。
- 使用 `{text=...}` 在音符处补充谱面文字。
- 使用 `{rest=N}` 连续休止 `N` 个小节。

---

### 9. 补充片段

补充片段写在所有音乐正文结束后。

#### 9.1 歌词块

| 元素 | 说明 |
| :--- | :--- |
| 空格分隔的词语 | 每个词语对应一个有音高的音符 |
| `%` | 无歌词占位，该音符不配歌词 |
| `-` | 英文单词拆音节 |

规则：
- 休止符 `0` 不占歌词位置
- 附点和延音不额外产生歌词位置
- 多个汉字无空格时视为整体附着到同一音符

遍次范围：

| 写法 | 含义 |
| :--- | :--- |
| `{lyrics=1}` | 仅第 1 遍 |
| `{lyrics=2,3}` | 第 2 和第 3 遍 |
| `{lyrics=1~3}` | 第 1 到第 3 遍 |

```m3n
{key=C} {4/4}
1 1 5 5 | 6 6 5^ |
4 4 3 3 | 2 2 1^ :|||

{lyrics}
一 闪 一 闪 亮 晶 晶
满 天 都 是 小 星 星
{/}
{lyrics}
Twin-kle twin-kle lit-tle star
How I won-der what you are!
{/}
```

#### 9.2 低音谱表

通过 `{bass}...{/}` 包裹的内容来补充低音谱表。
低音谱表使用与正文相同的音高、时值、小节线语法，应只包含谱表级内容（音符、时值、演奏标记等）。

```m3n
{key=D} {4/4}
3 3 4 5 | 5 4 3 2 | 1 1 2 3 | 2. (1) 1^ |||

{bass}
1d^^ | 5d^^ | 1d^^ | 5d^ 1d^ |||
{/}
```

---

### 10. 语法校验

本节列举 M3N 中所有非法语法。解析器应对以下情况报错，而非静默忽略。

#### 10.1 配对类

成对标记的闭合与匹配约束。

| 编号 | 非法语法 | 说明 |
| :--- | :--- | :--- |
| P1 | 小节内括号不匹配 | `()` 或 `[]` 在小节内未闭合 |
| P2 | 区间关闭缺失 | 区间起始标签无对应关闭标签 |
| P3 | 区间关闭冗余 | 区间关闭标签无对应起始标签 |
| P4 | 反复线不匹配 | 仅有前反复线 `||:` 而无后反复线 `:||` |

#### 10.2 结构类

文档与乐谱的整体结构约束。

| 编号 | 非法语法 | 说明 |
| :--- | :--- | :--- |
| S2 | 补充片段出现在正文之前 | `{lyrics}` 或 `{bass}` 必须在乐谱正文之后 |
| S3 | 空补充片段 | 歌词或低音谱表内容为空 |
| S4 | 段落引用不一致 | 段落声明与 `{parts=}` 引用无法严格匹配 |

#### 10.3 取值类

属性值的格式与范围约束。

| 编号 | 非法语法 | 说明 |
| :--- | :--- | :--- |
| V1 | 未知属性 | 不支持的属性名 |
| V2 | 调号格式非法 | 主音须为合法音名，调式后缀须为合法值 |
| V3 | 拍号格式非法 | 拍号分子必须为正整数，分母必须为 2 的正整数次幂 |
| V4 | 非法正整数取值 | 速度、反复数、连音时值、休止小节数等须为合法正整数 |
| V5 | 分组指令非法 | 分组冒号后必须为正整数或 `h` |
| V6 | 和弦名称非法 | 和弦级数无法解析 |
| V7 | 循环次数值非法 | 跳房子、歌词等循环次数相关语法无法解析 |

#### 10.4 语义类

语法合法但语义矛盾的约束。

| 编号 | 非法语法 | 说明 |
| :--- | :--- | :--- |
| M1 | 小节拍数不合规 | 小节必须满拍或满足弱起收束规则 |
| M2 | 延音目标非法 | `~` 后方无音符，或首个音符绝对音高与当前音符不同 |
| M3 | 休止符非法属性 | 休止符使用八度修饰、临时变音、延音、奏法等与音高相关的属性 |

#### 10.5 约束类

特定上下文中的限制规则。

| 编号 | 非法语法 | 说明 |
| :--- | :--- | :--- |
| R1 | 乐谱信息重复声明 | 同一字段只允许声明一次 |
| R2 | 时值后缀顺序错误 | 顺序必须为 `[^][.][~]` |
| R3 | 分组内含非法元素 | 分组内出现时值后缀、奏法等非音高修饰 |
| R4 | 分组音数不足 | 分组内至少需要两个音高 |
| R5 | 装饰音内含非法元素 | 装饰音内出现时值后缀、奏法等非音高修饰 |
| R6 | 空装饰音 | 装饰音内至少需要一个音高 |
