# M3N 手册

> [返回简介](README.md)

手册部分用于快速查询 M3N 的语法与约束，不再重复指南中的推导过程。
除非特别说明，下文中的"音符"均指一个完整的音符单元：音高部分 + 可选时值后缀 + 可选作用于它的后置属性。

## 1. 基本约定

### 1.1 文件与空白

- M3N 是纯文本格式。
- 空格、Tab、换行可作为分隔符使用。
- 连续空白不会改变语义，但会影响可读性。
- 渲染时会自动排版，但支持通过 `{br}` 强制换行。

### 1.2 注释

- `//` 表示行内注释。
- 从 `//` 开始直到行尾的内容均不参与解析。
- M3N 不支持块注释。

### 1.3 小节边界

- `|`：普通小节线
- `|||`：终止线
- `||:`：前反复记号
- `:||`：后反复记号
- `:||:`：双侧反复记号

- 圆括号 `()` 必须在同一小节内闭合。
- 音符分组 `[...]` 必须完整位于同一小节内。
- 延音 `~` 可以跨越小节线。
- 区间属性可以跨越多个小节和换行，但必须显式闭合。
- 若开头小节不满拍，渲染器可自动推断为弱起小节；M3N 不提供专门的弱起声明。

## 2. 属性模型

M3N 的属性统一使用大括号表示，基本形式如下：

```m3n
{key=value}
{flag}
```

其中 `key=value` 用于具名属性，`flag` 用于无值属性或自定义展示字段。

### 2.1 元信息属性

| 写法 | 含义 | 备注 |
| :--- | :--- | :--- |
| `{title=标题}` | 标题 | 通常显示在谱面顶部 |
| `{subtitle=副标题}` | 副标题 | 通常显示在标题下方 |
| `{composer=...}` | 作曲者 | 通常显示在标题右下方 |
| `{lyricist=...}` | 作词者 | 通常显示在作曲者下方 |
| `{arranger=...}` | 编曲者 | 通常显示在标题右下方 |
| `{copyright=...}` | 版权信息 | 通常显示在页脚 |
| `{source=...}` | 来源说明 | 通常显示在页脚或标题附近 |
| `{note=...}` | 备注 | 通常显示在谱面首行上方或页脚 |
| `{parts=A-B-A}` | 段落播放顺序 | 影响播放器的段落播放行为 |
| `{meta="..."}` | 自定义元信息 | 通常显示在标题右下角，用于上述字段无法覆盖的内容 |

同一字段可重复声明（例如多位作曲者），渲染器将按声明顺序合并显示。

### 2.2 状态属性

状态属性改变后续乐谱的解释方式：

| 写法 | 含义 |
| :--- | :--- |
| `{key=C}` | 调号（大调，与 `{1=C}` 等价） |
| `{key=Am}` | 调号（小调） |
| `{key=Ddor}` | 调号（教会调式） |
| `{1=C}` | 调号（历史写法，等价于 `{key=C}`，仍然有效） |
| `{4/4}` | 拍号 |
| `{tempo=120bpm}` | 速度 |
| `{part=A}` | 段落标记 |
| `{br}` | 换行（渲染提示） |
| `{rest=N}` | 多小节休止，`N` 为小节数 |
| `{text=内容}` | 谱面文字，附着在前一个元素上 |

规则：

- 状态属性从声明位置开始生效。
- 其作用范围持续到同类型状态属性再次出现，或文档结束。
- `{part=...}` 不影响任何音乐语义，不参与反复逻辑。
- `{br}` 放置在小节线后，指示渲染器在该处换行。
- `{rest=N}` 单独占据若干小节，不与其他音符混用。
- `{text=内容}` 不影响任何音乐语义。

### 2.3 结构属性

结构属性用于反复和跳转标记：

| 写法 | 含义 |
| :--- | :--- |
| `{segno}` / `{segno=A}` | Segno 标记 |
| `{fine}` | Fine 终点 |
| `{DC}` | Da Capo |
| `{DS}` / `{DS=A}` | Dal Segno（跳回最近的或指定名称的 segno） |

规则：

- 结构属性本身不形成区间，不需要闭合。
- 本规范定义其记谱含义；播放展开细节按常规西方记谱习惯实现。

## 3. 音符语法

### 3.1 基本形式

一个普通音符的推荐形式可抽象为：

```text
<音级><临时变音><八度修饰><时值后缀>
```

其中只有 `音级` 必填，其余部分均为可选。

### 3.2 音高部分

| 元素 | 写法 | 说明 |
| :--- | :--- | :--- |
| 音级 | `1 2 3 4 5 6 7` | 当前调式下的调内音级 |
| 升号 | `#` | 可连续叠加，如 `1##` |
| 降号 | `b` | 可连续叠加，如 `7bb` |
| 还原号 | `=` | 用于抵消前文同小节临时变音 |
| 高八度 | `e` | 可叠加，如 `1ee` |
| 低八度 | `d` | 可叠加，如 `6dd` |

约束如下：

- 顺序必须为 `<音级><临时变音><八度修饰><时值后缀>`。
- 临时变音在同一小节内持续生效，到小节线处失效。
- 临时变音按叠加逻辑解释，而非覆盖逻辑。
- 八度修饰只作用于当前音符，不向后传播。

### 3.3 特殊音符

| 写法 | 含义 |
| :--- | :--- |
| `0` | 休止符 |

规则：

- `0` 可参与括号时值系统。
- `0` 可附加 `^`、`.`。
- `0` 不可附加 `~`，因为它没有音高。
- `0` 不允许附加任何音高相关属性。

## 4. 时值系统

### 4.1 基础时值

- 基础时值由拍号分母决定。
- 在 `{4/4}` 中，基础时值 `B` 为四分音符。
- 一个不带括号和后缀的基础音符，其时值比例记为 `N = 1`。
- 实际时值 = `N × B`。

### 4.2 括号减时

M3N 使用圆括号 `()` 表示时值二分：

- 每嵌套一层括号，当前层内每个音符时值减半。
- 括号必须在小节内配对闭合。
- 括号除控制时值外，也承担节奏分组的可读性功能。

### 4.3 时值后缀

| 后缀 | 含义 | 规则 |
| :--- | :--- | :--- |
| `^` | 翻倍 | 每增加一个 `^`，当前时值乘以 2 |
| `.` | 附点 | 每增加一个 `.`，增加前一步时值的一半 |
| `~` | 延音 | 与后方第一个绝对音高完全相同的音符合并时值 |

补充规则：

- `^` 与 `.` 可以连续叠加。
- 同时使用时，顺序必须先 `^` 后 `.`。
- `~` 连接的两个音符必须具有完全相同的绝对音高。
- `0` 不允许使用 `~`，因为它没有音高。
- `~` 可以跨小节，也可以连续形成多段延音链。

### 4.4 多小节休止

```text
{rest=N}
```

- `N` 为正整数，表示连续休止的小节数。
- 渲染为带有小节数字的整块休止符。
- `{rest=N}` 独立占据对应数量的小节，不与其他音符混用。

## 5. 音符后置属性

音符后置属性使用大括号附着在前一个音符之后：

```m3n
1{tr}
1{ac(2)}
1{ap(2)}
1{ac+((2 3))}
```

### 5.1 装饰音

| 写法 | 名称 | 含义 |
| :--- | :--- | :--- |
| `{ac(...)}` | 前短倚音（acciaccatura） | 时值极短，渲染为带斜线小音符，主音时值不变 |
| `{ac+(...)}` | 后短倚音 | 时值极短，渲染为带斜线小音符，主音时值不变 |
| `{ap(...)}` | 前长倚音（appoggiatura） | 占用主音时值，渲染为不带斜线小音符 |
| `{ap+(...)}` | 后长倚音 | 占用主音时值，渲染为不带斜线小音符 |

规则：

- 倚音内容按"单音符或音符组"解析。
- 倚音可使用简化时值系统确定自身所占比例。
- 长倚音默认占用主音时值的一半；可在内部通过时值系统显式指定。
- 倚音附着于前一个主音，不独立占据外层语法位置。
- 每个主音符至多附着一个装饰音属性（短倚音与长倚音不可同时附着）。

### 5.2 奏法记号

| 写法 | 含义 |
| :--- | :--- |
| `{tr}` | 颤音 |
| `{wav}` / `{wav+}` | 上波音（二者等价） |
| `{wav-}` | 下波音 |
| `{echo}` | 回音 |
| `{str}` | 重音 |
| `{brk}` | 顿音 |
| `{tip}` | 跳音 |
| `{hold}` | 保持音 |
| `{breath}` | 呼吸记号 |

规则：

- 每个主音符至多附着一个奏法记号（`{breath}` 除外）。
- `{breath}` 可与其他奏法记号并存；可附着于音符后，也可附着于小节线后。
- 装饰音属性与奏法记号可以并存。
- 同一类别的后置属性不可重复。

## 6. 区间属性

部分属性不是作用于单个音符，而是作用于一段连续区域。

### 6.1 支持的区间属性

| 写法 | 含义 |
| :--- | :--- |
| `{lg}` | 连奏记号起点 |
| `{cresc}` | 渐强起点 |
| `{decres}` | 渐弱起点 |
| `{8va}` | 高八度记号起点 |
| `{8vb}` | 低八度记号起点 |
| `{volta=1}` | 跳房子 1 |
| `{volta=2}` | 跳房子 2 |

规则：

- 区间属性必须显式闭合。
- `{/}` 关闭最近一个尚未闭合的区间属性。
- `{/属性名}` 语义更清晰，也更利于校验。
- 允许嵌套时，应遵循后开先关。

### 6.2 即时属性

即时属性作用于当前位置之后的演奏表达，持续范围直到被下一个同类型属性覆盖，或文档结束。

以下力度记号属于即时属性：

`ppp`、`pp`、`p`、`mp`、`mf`、`f`、`ff`、`fff`、`fp`、`sfz`

## 7. 音符分组

M3N 使用中括号对一组纯音高音符进行整体处理：

```text
[notes:?]
```

其中：

- `notes` 是一组仅含音高词法单元的音符
- `?` 是分组模式

约束如下：

- 分组内允许的内容只有音级、临时变音和八度修饰。
- 分组内不允许出现 `0`、时值后缀、后置属性、括号层级或其他分组。
- 分组必须完整位于同一小节内。
- 分组作为一个整体，占据当前语法位置的一个时值单元。

### 7.1 连音组

当 `?` 为数字 `X` 时，表示组内音符均分 `X × 当前 N` 的总时值。

示例：

```m3n
[1 2 3:2]
```

表示一个三连音组，总时值等于当前两份最小单位。

### 7.2 和声组

当 `?` 为 `h` 时，表示组内音符同时发声。

示例：

```m3n
[1 3 5:h]
```

其整体时值等于当前位置的当前时值。

## 8. 反复与结构跳转

### 8.1 小节线反复

- `||:` 与 `:||` 成对构成局部反复。
- `:||:` 可视为后反复结束后立即进入新的前反复。
- 反复次数、展开规则若未特别说明，按常规西方记谱习惯解释。

### 8.2 结构跳转

支持以下结构元素：

- `segno` / `{segno=A}`
- `fine`
- `DC`
- `DS` / `{DS=A}`

说明：

- `{DC}` 表示返回开头。
- `{DS}` 表示返回最近的 `segno`；`{DS=A}` 表示返回指定名称的 `segno`。
- `{fine}` 用于标记结构反复的终止位置。

### 8.3 跳房子

- `{volta=n}` 开启第 `n` 个房子。
- 跳房子通常与 `:||` 配合使用。
- 一个跳房子区间应覆盖一段完整、可替换的末尾乐句。

## 9. 歌词

歌词块是音乐正文结束后的独立补充片段，使用 `{lyrics}...{/}` 或 `{lyrics=范围}...{/}` 包裹。

### 9.1 歌词块语法

```text
{lyrics} 歌词内容 {/}
{lyrics=1} 第1遍歌词 {/}
{lyrics=1,2} 第1、2遍通用歌词 {/}
{lyrics=1,3~5} 第1遍及第3至5遍通用歌词 {/}
```

### 9.2 歌词内容规则

| 元素 | 说明 |
| :--- | :--- |
| 空格分隔的词语 | 每个词语对应一个有音高的音符，按顺序一一对应 |
| `%` | 占位符，表示该音符无歌词，但仍消耗一个音符位置 |
| `-` | 连字符，用于将英文单词拆分至多个音符，如 `hap-py` |

补充规则：

- 休止符 `0` 不占用歌词位置。
- 连字符 `-` 拆分的各音节分别对应一个音符。
- 多个汉字不含空格时，视为整体附着于同一音符。
- 歌词对应关系以**实际发声的独立音符**为准（附点、延音等不产生额外的歌词位置）。

### 9.3 遍次范围语法

遍次范围用于指定歌词适用的演奏遍次，与跳房子编号逻辑一致：

- 单个数字：`1`
- 枚举：`1,2,4`
- 范围：`2~5`
- 混合：`1,3~5`

## 10. 语法速查

### 10.1 最小可用示例

```m3n
{key=C} {4/4}
1 2 3 4 | 5 6 7 1e |||
```

### 10.2 常见构件速查

| 目标 | 写法 |
| :--- | :--- |
| 调号（大调） | `{key=C}` 或 `{1=C}` |
| 调号（小调） | `{key=Am}` |
| 调号（教会调式） | `{key=Ddor}` |
| 拍号 | `{4/4}` |
| 标题 | `{title=标题}` |
| 副标题 | `{subtitle=副标题}` |
| 作曲者 | `{composer=贝多芬}` |
| 作词者 | `{lyricist=席勒}` |
| 编曲者 | `{arranger=...}` |
| 版权 | `{copyright=...}` |
| 来源 | `{source=...}` |
| 备注 | `{note=...}` |
| 段落顺序 | `{parts=A-B-A}` |
| 速度 | `{tempo=120bpm}` |
| 段落标记 | `{part=A}` |
| 换行 | `{br}` |
| 多小节休止 | `{rest=8}` |
| 谱面文字 | `{text="Andante"}` |
| 普通音符 | `5` |
| 升音 | `4#` |
| 高八度 | `1e` |
| 休止符 | `0` |
| 八分音符 | `(5)` |
| 二分音符 | `5^` |
| 附点音符 | `5.` |
| 延音 | `5~ 5` |
| 前短倚音 | `5{ac(6)}` |
| 前长倚音 | `5{ap(6)}` |
| 后短倚音 | `5{ac+(6)}` |
| 后长倚音 | `5{ap+(6)}` |
| 上波音 | `1{wav+}` |
| 下波音 | `1{wav-}` |
| 呼吸记号 | `1{breath}` |
| 和弦 | `[1 3 5:c]` |
| 三连音 | `[1 2 3:2]` |
| 连奏区间 | `{lg}1 2 3{/}` |
| 渐强区间 | `{cresc}1 2{/}` |
| 前反复 | `\|\|:` |
| 后反复 | `:\|\|` |
| 跳房子 | `{volta=1} ... {/}` |
| 歌词（全部） | `{lyrics} 词1 词2 {/}` |
| 歌词（指定遍次） | `{lyrics=1} 词1 词2 {/}` |

## 11. 规范性建议

- 每个文档开头显式声明调号与拍号。
- 优先使用 `{key=...}` 调式声明代替大量临时变音，尤其在小调旋律中。
- 音符之间保留适量空格，不依赖"无空格紧凑写法"。
- 区间属性优先使用 `{/属性名}` 闭合，减少歧义。
- 对复杂节奏优先增加括号层次，而不是依赖读者心算。
- 对复杂结构优先使用小节线反复，谨慎使用 `DC/DS`。
- 尽量让每一行在视觉上构成完整乐句，提升"所见即所得"的可读性。
- 歌词块统一放置在音乐正文结束后，作为独立补充片段编写，避免与正文混排。
- 多段歌词时，优先为每段显式标注遍次，避免歧义。
- 多段落乐曲建议在文档开头声明 `{parts=...}`，帮助读者把握整体结构。
