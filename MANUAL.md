# M3N 手册

> [返回简介](README.md)

本手册是 M3N 的完整语法参考，面向已掌握基本用法的用户。首次使用请先阅读 [M3N 指南](GUIDE.md)。

---

### 设计原则

**首调音级制谱**：音级 `1 2 3 4 5 6 7` 对应首调的 do-re-mi-fa-so-la-si，调号决定具体音高。

**小节为核心**：
- 圆括号、音符分组等语法结构必须在同一小节内闭合
- 临时变音在同小节内持续生效，到小节线处失效

**正文先写，补充后置**：音乐正文完整书写后，歌词、低音谱表等作为独立补充片段追加。

---

### 1. 文档结构

#### 1.1 文件格式

- 纯文本格式，扩展名 `.m3n`
- 空格、Tab、换行在语法解析时无含义
- `//` 从该符号到行尾均为注释，不参与解析

#### 1.2 文档组成

```
元信息（可选）
音乐正文（调号 → 拍号 → 音符 → 小节线 → 终止线）
补充片段（可选，歌词块、低音谱表块）
```

示例：

```m3n
{title=标题} {composer=作曲者}

{key=Cmaj} {4/4} {tempo=120bpm}
1 2 3 4 | 5 6 7 1e |||

{lyrics}
词 语 词 语 词 语 词 语
{/}

{bass}
1d 5d 1 5d | 1d^^ |||
{/}
```

- `|||` 是终止线，若同时需要后反复则写作 `:|||`
- 补充片段写在正文结束后，不改变正文音符顺序

#### 1.3 属性形式

属性统一使用大括号，有三种形式：

```m3n
{key=Cmaj}        // key=value 形式
{4/4}             // flag 形式（无 value）
{/}               // 匿名闭合标签
{/volta}          // 具名闭合标签
```

---

### 2. 乐谱信息

| 属性 | 含义 |
| :--- | :--- |
| `{title=...}` | 标题 |
| `{subtitle=...}` | 副标题 |
| `{composer=...}` | 作曲者 |
| `{lyricist=...}` | 作词者 |
| `{arranger=...}` | 编曲者 |
| `{copyright=...}` | 版权 |
| `{source=...}` | 来源 |
| `{note=...}` | 备注 |

同一字段可重复声明，渲染器按顺序合并。

---

### 3. 乐曲设置

乐曲设置从声明位置生效，持续到同类设置再次出现或文档结束。

#### 3.1 调号

```m3n
{key=C}      // C 大调（等同于 Cmaj）
{key=Cmaj}   // C 大调
{key=Am}     // A 自然小调
{key=Ddor}   // D 多利亚调式
```

调式后缀：

| 后缀 | 调式 | 示例 |
| :--- | :--- | :--- |
| 无 | 自然大调 | `{key=C}` = `{key=Cmaj}` |
| `m` | 自然小调 | `{key=Am}` |
| `dor` | 多利亚 | `{key=Ddor}` |
| `phr` | 弗里几亚 | `{key=Ephr}` |
| `lyd` | 利底亚 | `{key=Flyd}` |
| `mix` | 混合利底亚 | `{key=Gmix}` |
| `loc` | 洛克里亚 | `{key=Bloc}` |

#### 3.2 拍号

| 写法 | 含义 |
| :--- | :--- |
| `{4/4}` | 四四拍 |
| `{3/4}` | 四三拍 |
| `{2/4}` | 四二拍 |
| `{6/8}` | 八六拍 |

#### 3.3 速度

```m3n
{tempo=120bpm}
```

#### 3.4 播放移调

```m3n
{transpose=-2}
```

`transpose` 的值为半音数，只影响播放音高，不改变 M3N 音符内容或五线谱显示。负数降低，正数升高；省略或设为 `0` 时不移调。

| 示例 | 值 | 播放效果 |
| :--- | :--- | :--- |
| Bb 单簧管 | `{transpose=-2}` | 谱面 C 播放为降 B |
| Eb 中音萨克斯 | `{transpose=-9}` | 谱面 C 播放为降 E |
| 升高全音 | `{transpose=2}` | 所有播放音高升高两个半音 |

---

### 4. 音符

#### 4.1 音符结构

完整形式：`<音级><临时变音><八度修饰><时值后缀>`

只有音级必填。示例：

```m3n
1           // 只有音级
4#          // 升号
7bd         // 降号，降两次
1e^.        // 高八度 + 翻倍 + 附点
```

#### 4.2 音高

| 元素 | 写法 | 说明 |
| :--- | :--- | :--- |
| 音级 | `1 2 3 4 5 6 7` | 首调音级 |
| 升号 | `#` | 升高半音，可叠加 `4##` |
| 降号 | `b` | 降低半音，可叠加 `7bb` |
| 还原号 | `=` | 还原同小节前面的临时变音 |
| 高八度 | `e` | elevate，升高一个八度，可叠加 |
| 低八度 | `d` | descend，降低一个八度，可叠加 |
| 休止符 | `0` | 无音高，参与时值计算 |

规则：
- 顺序：音级 → 临时变音 → 八度修饰 → 时值后缀
- 临时变音在同小节持续生效，小节线处失效
- 八度修饰只作用于当前音符
- `0` 休止符可用 `^` 和 `.`，不可用 `~`

#### 4.3 时值

拍号分母决定基础时值（四分音符 = 1）。

| 写法 | 含义 | 以四分音符为基准 |
| :--- | :--- | :--- |
| `1` | 基础时值 | 1 倍 |
| `(1)` | 括号内时值减半 | 1/2 倍 |
| `((1))` | 减半两次 | 1/4 倍 |
| `(((1)))` | 减半三次 | 1/8 倍 |
| `1^` | 时值翻倍 | 2 倍 |
| `1^^` | 翻倍两次 | 4 倍 |
| `1.` | 附点 | 1.5 倍 |
| `1^.` | 先翻倍后附点 | 3 倍 |
| `5~ 5` | 延音 | 连接后方同音高音符 |

规则：
- 每层圆括号时值减半，必须在同一小节内闭合
- `^` 和 `.` 可叠加，必须先 `^` 后 `.`
- `~` 可跨小节，目标必须是后方第一个绝对音高相同的音符
- `0` 休止符可用 `^` 和 `.`，不可用 `~`

---

### 5. 小节线

| 写法 | 含义 |
| :--- | :--- |
| `|` | 普通小节线 |
| `||` | 双小节线（段落标记） |
| `|||` | 终止线 |
| `||:` | 前反复起点 |
| `:||` | 后反复终点 |
| `:|||` | 后反复与终止线合并 |
| `:||:` | 后反复结束后立即进入新前反复 |

---

### 6. 音符分组

分组语法：`[notes:mode]`，组内只写音高词法单元。

| 写法 | 含义 | 说明 |
| :--- | :--- | :--- |
| `[1 3 5:h]` | 和声组 | 音符同时发声，占 1 份时值 |
| `[1 2 3:2]` | 连音组 | 3 音均分 2 份时值 |

规则：分组必须完整位于同一小节内；组内不允许休止符、时值后缀、后置属性、圆括号或嵌套分组。

---

### 7. 演奏标记

#### 7.1 力度记号

即时属性，从当前位置生效直到被新的力度覆盖。

| 写法 | 含义 | 写法 | 含义 |
| :--- | :--- | :--- | :--- |
| `{ppp}` | 极弱 | `{fp}` | 强后即弱 |
| `{pp}` | 很弱 | `{sfz}` | 特强 |
| `{p}` | 弱 | | |
| `{mp}` | 中弱 | | |
| `{mf}` | 中强 | | |
| `{f}` | 强 | | |
| `{ff}` | 很强 | | |
| `{fff}` | 极强 | | |

#### 7.2 后置属性

附着在前一个音符之后，不向后传播。

**奏法记号**：

| 写法 | 含义 | 写法 | 含义 |
| :--- | :--- | :--- | :--- |
| `{tr}` | 颤音 | `{echo}` | 回音 |
| `{str}` | 重音 | `{hold}` | 保持音 |
| `{brk}` | 顿音 | `{breath}` | 呼吸记号 |
| `{tip}` | 跳音 | `{f1}`~`{f5}` | 指法 |
| `{wav}` | 上波音 | | |
| `{wav+}` | 上波音变体 | | |
| `{wav-}` | 下波音 | | |

**装饰音**：

| 写法 | 名称 | 说明 |
| :--- | :--- | :--- |
| `{ac(...)}` | 前短倚音 | acciaccatura，带斜线小音符，主音时值不变 |
| `{ap(...)}` | 前长倚音 | appoggiatura，不带斜线，占用主音时值 |

规则：每个主音至多附着一个奏法记号和一个装饰音；两者可并存；`{breath}` 可附着于小节线后。

#### 7.3 区间属性

作用于一段连续内容，必须显式闭合。

| 写法 | 含义 | 闭合方式 |
| :--- | :--- | :--- |
| `{lg}` | 连奏区间 | `{/}` 或 `{/lg}` |
| `{cresc}` | 渐强区间 | `{/}` 或 `{/cresc}` |
| `{decres}` | 渐弱区间 | `{/}` 或 `{/decres}` |
| `{8va}` | 高八度区间 | `{/}` 或 `{/8va}` |
| `{8vb}` | 低八度区间 | `{/}` 或 `{/8vb}` |
| `{volta=N}` | 第 N 房子 | `{/}` 或 `{/volta}` |
| `{volta=N,M}` | 第 N、M 房子 | `{/}` 或 `{/volta}` |
| `{volta=N~M}` | 第 N 到 M 房子 | `{/}` 或 `{/volta}` |

示例：

```m3n
{lg}1 2 3 4{/lg}
{cresc}1 2 | 3 4{/cresc}
{volta=1}3. (2) 2^ {/}:||
{volta=2}2. (1) 1^ {/}|||
```

---

### 8. 结构与反复

#### 8.1 小节线反复

```m3n
||: 1 2 3 4 | 5 6 7 1e :||   // 反复一次
||: 1 2 3 4 | 5 6 7 1e :|||  // 反复一次并结束
```

播放逻辑：
1. 遇到 `||:` 记住位置
2. 遇到 `:||` 或 `:|||` 时跳回记住的位置
3. 再次遇到 `||:` 清除记住的位置

#### 8.2 跳房子

当反复段每次演奏方式不同时使用：

```m3n
||: 3 3 4 5 | 5 4 3 2 | 1 1 2 3 | {volta=1} 3. (2) 2^ {/}:||
{volta=2} 2. (1) 1^ {/}|||
```

#### 8.3 段落标记

`{parts=A B A C}` 转换为 ABC 文件头中的 `P:A B A C`。

```m3n
{key=D} {4/4}
{parts=A B A C}

{part=A}
3 3 4 5 | 5 4 3 2 ||
{/}

{part=B}
1 1 2 3 | 3. (2) 2^ ||
{/}

{part=C}
1 1 2 3 | 2. (1) 1^ ||
{/}
```
播放器按 `A-B-A-C` 顺序播放各段落。

#### 8.4 结构跳转

| 写法 | 含义 |
| :--- | :--- |
| `{segno}` | Segno 标记 |
| `{segno=A}` | 具名 Segno |
| `{fine}` | Fine 终点 |
| `{DC}` | Da Capo，回到开头 |
| `{DS}` | Dal Segno，回到最近 Segno |
| `{DS=A}` | Dal Segno，回到指定 Segno |

---

### 9. 排版与标注

#### 9.1 排版属性

| 写法 | 含义 | 持续性 |
| :--- | :--- | :--- |
| `{br}` | 换行提示 | 只作用于当前位置 |
| `{part=A}...{/part}` | 段落标记 | 成对使用 |
| `{parts=A B A}` | 段落顺序 | 声明在正文之前 |
| `{rest=N}` | 多小节休止 | 独立占据 N 小节 |

#### 9.2 谱面标注

| 写法 | 含义 | 持续性 |
| :--- | :--- | :--- |
| `{text=rit.}` | 谱面文字 | 只作用于当前位置 |
| `{chord=Am}` | 和弦标记 | 持续到本小节下一个和弦标记或小节末尾 |

---

### 10. 补充片段

补充片段写在所有音乐正文结束后。

#### 10.1 歌词块

```m3n
{key=C} {4/4}
1 1 5 5 | 6 6 5^ |
4 4 3 3 | 2 2 1^ |||

{lyrics}
一 闪 一 闪 亮 晶 晶
满 天 都 是 小 星 星
{/}
```

| 元素 | 说明 |
| :--- | :--- |
| 空格分隔的词语 | 每个词语对应一个有音高的音符 |
| `%` | 无歌词占位，该音符不配歌词 |
| `-` | 英文单词拆音节 |

规则：
- 休止符 `0` 不占歌词位置
- 附点和延音不额外产生歌词位置
- 多个汉字无空格时视为整体附着到同一音符

遍次范围：

| 写法 | 含义 |
| :--- | :--- |
| `{lyrics=1}` | 仅第 1 遍 |
| `{lyrics=2,3}` | 第 2 和第 3 遍 |
| `{lyrics=1~3}` | 第 1 到第 3 遍 |

#### 10.2 低音谱表块

```m3n
{key=D} {4/4}
3 3 4 5 | 5 4 3 2 | 1 1 2 3 | 2. (1) 1^ |||

{bass}
1e^^ | 5e^^ | 1e^^ | 5e^ 1e^ |||
{/}
```

低音谱表使用与正文相同的音高、时值、小节线语法，应只包含谱表级内容（低音区音、和声组）。

---

### 附录：约束速查

| 语法元素 | 约束 |
| :--- | :--- |
| 圆括号 `()` | 必须在同一小节内闭合 |
| 中括号分组 `[...]` | 必须在同一小节内 |
| 临时变音 | 在同小节内持续生效，小节线处失效 |
| 八度修饰 | 只作用于当前音符 |
| 延音 `~` | 目标必须是后方第一个绝对音高相同的音符 |
| 区间属性 | 必须显式闭合 |

---

### 附录：推荐写法

- 文档开头先写标题等元信息；音乐正文开头显式声明调号、拍号和速度
- 每行尽量对应一个乐句或一个清晰片段
- 小节线前后保留空格以提升可读性
- 复杂区间优先使用具名闭合 `{/lg}`、`{/cresc}`、`{/volta}`
- 复杂节奏优先用圆括号表达层级
- 选择符合乐曲调式的调号写法，避免大量临时升降号
