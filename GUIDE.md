# M3N 指南

> [返回简介](README.md)

这份指南面向第一次使用 M3N 的制谱者。它不会按语法分类逐条解释，而是按实际写谱时的学习顺序展开：每一节只引入少量新元素，并用一个可运行的片段完成一个明确目标。

需要查完整规则时，请转到 [M3N 手册](MANUAL.md)。

## 开始写谱

这一部分用于完成一份最小可读、可播放、可打印的旋律谱。学完之后，你可以写出普通单旋律、长短音、休止、标题信息和简单换行。

### 写出第一条旋律

本节新增元素：

- `{key=C}`：调号，表示 C 大调。
- `{4/4}`：拍号，表示四四拍。
- `1 2 3 4 5 6 7`：当前调内的七个音级。
- `|`：小节线。
- `|||`：终止线。

一份 M3N 乐谱可以从调号、拍号和音符开始：

```m3n
{key=C} {4/4}
1 2 3 4 | 5 6 7 1 |||
```

### 完成《小星星》的骨架

本节新增元素：

- `^`：把当前音符时值翻倍。

在 `{4/4}` 中，普通音符默认是四分音符。`5^` 就是把 `5` 拉长成二分音符。

```m3n
{key=C} {4/4}
1 1 5 5 | 6 6 5^ |
4 4 3 3 | 2 2 1^ |||
```

继续把后面的乐句补上，就得到完整的《小星星》：

```m3n
{key=C} {4/4}
1 1 5 5 | 6 6 5^ |
4 4 3 3 | 2 2 1^ |
5 5 4 4 | 3 3 2^ |
5 5 4 4 | 3 3 2^ |
1 1 5 5 | 6 6 5^ |
4 4 3 3 | 2 2 1^ |||
```

音符之间的空格不是语法必需，但建议保留。对使用者来说，M3N 的可读性比压缩字符更重要。

### 写出更短的音和休止

本节新增元素：

- `( ... )`：括号内音符时值减半。
- `0`：休止符。

如果普通音符是四分音符，`(5)` 就是八分音符。下面这段乐句用短音做经过音，并用 `0` 留出停顿：

```m3n
{key=C} {4/4}
1 (2 3) 4 0 | 5 (6 5) 3 1 |
0 (1 2) 3 4 | 5 0 1^ |||
```

括号可以嵌套，每多一层就再减半：

```m3n
{key=C} {4/4}
1 (2 3) ((4 5 6 7)) 1 | 1^ 0^ |||
```

小节是组织音乐内容的核心单元。为了保持小节的独立性，M3N 要求括号必须在同一小节内闭合，不能跨过小节线。

### 加上标题、作者和速度

本节新增元素：

- `{title=...}`：标题。
- `{subtitle=...}`：副标题。
- `{composer=...}`：作曲者。
- `{tempo=120bpm}`：速度。
- `//`：行内注释。

这些信息通常放在乐谱开头：

```m3n
{title=小星星} {subtitle=《C大调变奏曲主题》}
{composer=莫扎特}
{key=C} {4/4} {tempo=100bpm}
1 1 5 5 | 6 6 5^ | // 第一乐句
4 4 3 3 | 2 2 1^ |||
```

从 `//` 开始直到行尾的文字不会参与渲染，适合写给自己看的备注。

### 控制谱面换行

本节新增元素：

- `{br}`：建议渲染器在这里换行。

渲染器通常会自动排版。需要明确分句时，可以把 `{br}` 放在小节线后：

```m3n
{title=小星星}
{key=C} {4/4}
1 1 5 5 | 6 6 5^ |{br}
4 4 3 3 | 2 2 1^ |{br}
5 5 4 4 | 3 3 2^ |||
```

这不会改变音乐内容，只影响排版效果。

## 写出完整旋律

这一部分用于补齐真实旋律中常见的变化：附点、延音、升降音、八度、歌词和常见演奏标记。学完之后，你可以写出更像正式乐谱的单旋律。

### 写附点节奏

本节新增元素：

- `.`：附点，增加当前时值的一半。

`5.` 比 `5` 更长；`5. (6)` 是常见的附点加短音节奏：

```m3n
{key=C} {4/4}
1. (2) 3. (1) | 3 1 3^ |||
```

`^` 和 `.` 可以配合使用，但顺序是先 `^` 后 `.`：

```m3n
{key=C} {4/4}
5^. 0 | 1e^^ |||
```

### 跨小节延长同一个音

本节新增元素：

- `~`：延音线，连接后方第一个绝对音高相同的音符。

当一个音跨过小节线，或者需要分成多个可读时值时，使用 `~`：

```m3n
{key=C} {4/4}
1 1~ | 1 1~ | 1^~ | 1^ |||
```

`~` 只能用于有音高的音符，不能用于休止符 `0`。

### 写高低八度

本节新增元素：

- `e`：当前音符高八度，来自英文 `elevated`。
- `d`：当前音符低八度，来自英文 `dropped`。

八度修饰只影响当前音符：

```m3n
{key=C} {4/4}
1 2 3 4 | 5 6 7 1e |
1e 7 6 5 | 4 3 2 1 |||
```

低音区可以用 `d`：

```m3n
{key=C} {4/4}
5d 6d 7d 1 | 2 3 4 5 |||
```

### 写临时升降音

本节新增元素：

- `#`：升高半音。
- `b`：降低半音。
- `=`：还原同小节前面的临时变音。

临时变音写在音级后、八度修饰前：

```m3n
{key=G} {4/4}
7 7# 1e 7 | 6 5 4# 5 |||
```

如果同一小节里需要回到未变化的音，可以使用 `=`：

```m3n
{key=C} {4/4}
4# 4# 4= 3 | 2 1 0^ |||
```

### 选择调式

本节新增元素：

- `{key=Am}`：A 自然小调。
- `{key=Ddor}` 等：教会调式。

如果旋律本来就是小调，直接声明小调比在大调里反复写升降号更清楚：

```m3n
{key=Am} {4/4}
1 2 3 4 | 5 6 7 1e |
7 6 5 4 | 3 2 1^ |||
```

常用写法包括：

| 目标 | 写法 |
| :--- | :--- |
| 大调 | `{key=C}` |
| 自然小调 | `{key=Am}` |
| 多利亚 | `{key=Ddor}` |
| 弗里几亚 | `{key=Ephr}` |
| 利底亚 | `{key=Flyd}` |
| 混合利底亚 | `{key=Gmix}` |
| 洛克里亚 | `{key=Bloc}` |

### 添加连奏、力度和文字

本节新增元素：

- `{lg} ... {/}`：连奏区间。
- `{p}`、`{mf}`、`{f}` 等：力度。
- `{cresc} ... {/}`、`{decres} ... {/}`：渐强、渐弱区间。
- `{text=...}`：谱面文字。
- `{chord=...}`：和弦标记。

区间类标记需要用 `{/}` 关闭：

```m3n
{key=C} {4/4}
{p}{lg}1 2 3 4{/} | {mf}5 6 7 1e |
{cresc}1e 7 6 5{/} | {f}{text=rit.}4 3 2 1 |||
```

和弦标记可以放在对应位置：

```m3n
{key=C} {4/4}
{chord=C}1 2 3 4 | {chord=G}5 6 7 1e |||
```

### 给旋律配歌词

本节新增元素：

- `{lyrics} ... {/}`：歌词块。
- `%`：无歌词占位。
- `-`：英文拆音节。

歌词不是夹在旋律中逐音书写，而是在所有音乐正文结束后，作为一个独立的补充片段添加。每个以空格分隔的词语对应一个有音高的音符，休止符不占歌词位置：

```m3n
{title=小星星}
{key=C} {4/4}
1 1 5 5 | 6 6 5^ |
4 4 3 3 | 2 2 1^ |||

{lyrics}
一 闪 一 闪 亮 晶 晶
满 天 都 是 小 星 星
{/}
```

英文歌词可以用连字符拆到多个音符：

```m3n
{key=C} {4/4}
1 1 5 5 | 6 6 5^ |||

{lyrics}
Twin-kle twin-kle lit-tle star
{/}
```

如果某个音没有歌词，可以用 `%` 占位：

```m3n
{key=C} {4/4}
1 1 5 5 | 6 6 5^ |||

{lyrics}
一 闪 % 闪 亮 晶 晶
{/}
```

## 组织复杂乐谱

这一部分用于处理真实曲谱中的结构和细节：反复、跳房子、段落、装饰音、连音、和声、多小节休止和低音谱表。学完之后，你可以把较长乐曲组织得更清楚。

### 写反复段落

本节新增元素：

- `||:`：前反复。
- `:||`：后反复。
- `:||:`：后反复结束后接新的前反复。

最简单的反复是把需要重复的乐句包起来：

```m3n
{key=C} {4/4}
||: 1 1 5 5 | 6 6 5^ |
4 4 3 3 | 2 2 1^ :|| |||
```

如果一个反复结束后马上进入下一个反复，可以使用 `:||:`：

```m3n
{key=C} {4/4}
||: 1 2 3 4 | 5^ 0^ :||:
5 6 7 1e | 1e^ 0^ :|| |||
```

### 处理不同结尾

本节新增元素：

- `{volta=1} ... {/}`：第 1 房子。
- `{volta=2} ... {/}`：第 2 房子。

当反复的最后一小段第一遍和第二遍不同时，用跳房子：

```m3n
{key=C} {4/4}
||: 1 2 3 4 | 5 6 7 1e |
4 3 2 1 | {volta=1}5^ 0^ {/}:||
{volta=2}1^^ {/}|||
```

`volta` 是区间属性，记得关闭。为了可读性，也可以写成 `{/volta}`。

### 标记段落顺序

本节新增元素：

- `{part=A}`：正文中的段落标记。
- `{parts=A-B-A-C}`：文档开头的段落顺序说明。

较长歌曲可以先说明整体结构，再在正文中标出段落：

```m3n
{title=段落示例}
{parts=A-B-A-C}
{key=C} {4/4}

{part=A}
1 2 3 4 | 5 6 7 1e |

{part=B}
3e 2e 1e 7 | 6 5 4 3 |

{part=A}
1 2 3 4 | 5 6 7 1e |

{part=C}
{lg}5 4 3 2 1{/} | 1^^ |||
```

`{parts=...}` 用于声明段落播放顺序。

### 添加装饰音

本节新增元素：

- `{ac(...)}`：前短倚音。
- `{ap(...)}`：前长倚音。
- `{ac+(...)}`：后短倚音。
- `{ap+(...)}`：后长倚音。

装饰音附着在前一个主音后面：

```m3n
{key=C} {4/4}
1{ac(2)} 2 3{ac+(2)} 1 |
5{ap(6)} 4 3{ap+(2)} 1 |||
```

`ac` 来自 `acciaccatura`，表示短倚音，通常渲染为带斜线的小音符，主音时值不变。`ap` 来自 `appoggiatura`，表示长倚音，通常渲染为不带斜线的小音符，并占用主音时值。默认写在主音前，带 `+` 时写在主音后。

### 添加奏法记号

本节新增元素：

- `{tr}`：颤音。
- `{wav}`、`{wav+}`、`{wav-}`：上波音、下波音。
- `{str}`、`{brk}`、`{tip}`、`{hold}`：常见奏法。
- `{breath}`：呼吸记号。

奏法记号也附着在前一个音符后面：

```m3n
{key=C} {4/4}
1{tr} 2{wav} 3{wav-} 4 |
5{str} 6{brk} 7{tip} 1e{hold} |
1e{breath} 7 6 5 |||
```

`{breath}` 也可以附着在小节线后，表示小节间换气。

### 写三连音和其他连音

本节新增元素：

- `[notes:X]`：连音组，组内音符均分 `X` 份当前时值。

最常见的是三连音。下面的 `[1 2 3:2]` 表示三个音占用两份当前位置时值：

```m3n
{key=C} {3/4}
1 (1 2) ([1 2 3:2]) | 5^ 0 |||
```

组内只写音高，不写时值后缀、休止符或附着属性。

### 写简单和声音符

本节新增元素：

- `[notes:h]`：和声组，组内音符同时发声。

当同一个时值位置需要多个音同时发声时，可以使用和声组，例如旋律中的双音、三音，或低音谱表中的伴奏和声：

```m3n
{key=C} {3/4}
[1 3 5:h] [5 7 2e:h] [1 3 5:h] |||
```

和声组整体占用当前位置的一个时值单元。

### 使用多小节休止和低音谱表

本节新增元素：

- `{rest=N}`：多小节休止。
- `{bass} ... {/}`：低音谱表块。

多小节休止适合跳过较长空白段：

```m3n
{key=C} {4/4}
1 2 3 4 |
{rest=4}
5 6 7 1e |||
```

低音谱表不是夹在主旋律中逐小节书写，而是在乐谱正文编写完成后，作为一个独立段落添加：

```m3n
{title=双谱表示例}
{key=C} {4/4}
1 2 3 4 | 5 6 7 1e |||

{bass}
1d^ 5d^ | 1^ 0^ |
{/}
```

低音谱表的音高和时值写法与主旋律一致，但应只放谱表级内容。更细的限制请查手册。
