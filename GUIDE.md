# M3N 指南

> [返回简介](README.md)

这份指南面向第一次使用 M3N 的制谱者。它按实际写谱时的学习顺序展开，每节只引入少量新元素，并用一个可运行的片段完成一个明确目标。

需要查完整规则时，请转到 [M3N 手册](MANUAL.md)。

## 谱写简单旋律

这一部分学完之后，你可以写出由简单音高和节奏组成的旋律。

### 小节与节拍

**小节**是组成旋律的基础单位：
- `|` → 普通小节线 
- `||` → 双小节线
- `|||` → 终止线

**拍号**确定了一个小节内音符的骨架和呼吸：
| 元素 | 含义 |
| :--- | :--- |
|`{4/4}`|以`四分音符`为1拍，每小节有`四拍`|
|`{6/8}`|以`八分音符`为1拍，每小节有`六拍`|

### 简单音高

本节新增元素：
| 元素 | 含义 |
| :--- | :--- |
|`{key=C}`|调号，表示 C 大调。|
|`1 2 3 4 5 6 7`|当前调内的七个音级。|

**调号** `{key=C}` 同时确定了乐谱的调式和调性：
- 调性 `C` ，明确主音 `1` 的音高为 `中央C4`
- 默认调式 `自然大调` ，明确了 `1` 到 `7` 的音程排列方式为 `全全半全全全半`

有了调号、拍号、音符和小节，我们就能写出《欢乐颂》开头的旋律了：

```m3n
{key=C} {4/4}
3 3 4 5 | 5 4 3 2 |||
```

### 简单节奏

本节新增元素：
| 元素 | 含义 |
| :--- | :--- |
|`{120qpm}`|速度，每分钟的`四分音符`数量|
|`^`|时值翻倍，可叠加|
|`( ... )`|时值减半，可嵌套|
|`0`|休止符|

每分钟的`四分音符`数 `{120qpm}` 决定了每个`四分音符`的持续时长。在此基础上确定每个音符的时值，我们就能明确音符的持续时长了。

在四分音符 `5` 后插入时值翻倍符号 `5^`，拉长成二分音符，即可编写出完整的《小星星》：

```m3n
{key=C} {4/4} {108qpm}
1 1 5 5 | 6 6 5^ | 4 4 3 3 | 2 2 1^ |
5 5 4 4 | 3 3 2^ | 5 5 4 4 | 3 3 2^ |
1 1 5 5 | 6 6 5^ | 4 4 3 3 | 2 2 1^ |||
```

> 在音乐正文中，空白不改变音乐含义。建议通过合适的空格提升文本的可读性。


通过不同层级的括号包裹，可以将音符的时值减半，例如 `(5)` 就是八分音符，`((5))` 就是十六分音符。

使用休止符 `0` 来占据不发声的时值，休止符像所有的正常音符一样适用于时值系统。

结合括号和休止符，编写出完整的《洋娃娃和小熊跳舞》：

```m3n
{key=C} {2/4} {96qpm}
(1 2) (3 4) | (5 5) ((5 4) 3) | (4 4) ((4 3) 2) | (1 3) (5 0) |
(1 2) (3 4) | (5 5) ((5 4) 3) | (4 4) ((4 3) 2) | (1 3) (1 0) |
(6 6) ((6 5) 4) | (5 5) ((5 4) 3) | (4 4) ((4 3) 2) | (1 3) (5 0) |
(6 6) ((6 5) 4) | (5 5) ((5 4) 3) | (4 4) ((4 3) 2) | (1 3) (1 0) |||
```


### 简单信息

本节新增元素：
- `{title=...}`：标题
- `{subtitle=...}`：副标题
- `{composer=...}`：作曲者
- `//`：行内注释

这些信息通常放在乐谱开头：

```m3n
{title=小星星} {subtitle=《C大调变奏曲主题》}
{composer=莫扎特}
{key=C} {4/4} {108qpm}
// 待补充更多细节
1 1 5 5 | 6 6 5^ | 4 4 3 3 | 2 2 1^ |
5 5 4 4 | 3 3 2^ | 5 5 4 4 | 3 3 2^ |
1 1 5 5 | 6 6 5^ | 4 4 3 3 | 2 2 1^ |||
```

从 `//` 开始直到行尾的文字不参与渲染，适合写备注。

## 完善旋律系统

这一部分用于补齐真实旋律中常见的音高时值语法。包括音高中的**八度**、**临时变音**、**调式**，时值中的**附点**、**延音**、**和声**、**连音**。

### 音高-八度

本节新增元素：
- `e`：当前音符高八度，来自 elevate
- `d`：当前音符低八度，来自 descend

八度修饰只影响当前音符。下面是《送别》第一句的高八度音：

```m3n
{key=C} {4/4} {72qpm}
5 (3 5) 1e^ | 6 1e 5^ |||
```
下面是《生日快乐》中的低八度音：

```m3n
{key=C} {3/4}
(5d 5d) | 6d 5d 1 | 7d^   (5d 5d) | 6d 5d 2 | 1^ 
(5d 5d) | 5  3  1 | 7d 6d (4  4 ) | 3  1  2 | 1^ |||
```

### 音高-临时变音

本节新增元素：
- `#`：升高半音
- `b`：降低半音
- `=`：还原同小节前面的临时变音

临时变音写在音级后、八度修饰前，会在当前小节内持续影响相同音级，直到小节结束或遇到其他临时变音。以《致爱丽丝》的开头为例：

```m3n
{key=C} {3/8}
(3e 2#e) | (3e 2#e 3e 7 2=e 1e) | 6 0 |||
```

### 音高-调式

本节新增元素：

| 调式 | 写法 |
| :--- | :--- |
| 大调 | `{key=Cmaj}` |
| 多利亚 | `{key=Ddor}` |
| 弗里几亚 | `{key=Ephr}` |
| 利底亚 | `{key=Flyd}` |
| 混合利底亚 | `{key=Gmix}` |
| 自然小调 | `{key=Am}` |
| 洛克里亚 | `{key=Bloc}` |

虽然大调和小调在表达上没有本质区别，但是在《绿袖子》的示例中，乐曲结束在主音 `1` 上会更加清晰合理：

```m3n
{key=Em} {3/4}
7^. | 7. (6#) 5 | 4^ 2 | 7d. (1) 2 |
3. (2) 1 | 7#d. (6#d) 7d | 1^. | 1^. |||
```

### 时值-附点

本节新增元素：
- `.`：附点，增加当前时值的一半

`5.` 比 `5` 更长；附点加短音是很多主题句的收束方式。下面是《欢乐颂》第一句：

```m3n
{key=D} {4/4}
3 3 4 5 | 5 4 3 2 | 1 1 2 3 | 3. (2) 2^ |||
```

> `^` 和 `.` 可以配合使用，必须按照先 `^` 后 `.`的顺序：

```m3n
{key=C} {4/4}
5^. 0 | 1e^^ |||
```

### 时值-延音

本节新增元素：
- `~`：延音记号，连接后方第一个绝对音高相同的音符

当一个音跨过小节线，或者需要分成多个可读时值时，使用 `~`。
《欢乐颂》中，贝多芬通过一个延音的巧思，打破了平淡，创造了张力：

```m3n
{title=欢乐颂}
{key=D} {4/4} {120qpm}
3 3 4 5 | 5 4 3 2 | 1 1 2 3 | 3. (2) 2^ |
3 3 4 5 | 5 4 3 2 | 1 1 2 3 | 2. (1) 1^ |
2 2 3 1 | 2 (3 4) 3 1 | 2 (3 4) 3 2 | 1 2 5d 3~ |
3 3 4 5 | 5 4 3 2 | 1 1 2 3 | 2. (1) 1^ |||
```

### 时值-和声

本节新增元素：
- `[notes:h]`：和声组，组内音符同时发声

当同一个时值位置需要多个音同时发声时，使用和声组。和声组整体占用当前位置的一个时值单元。这是三度和声版本的《欢乐颂》：

```m3n
{title=欢乐颂}
{key=D} {4/4} {120qpm}
[1 3:h] [1 3:h] [2 4:h] [3 5:h] |
[3 5:h] [2 4:h] [1 3:h] [7d 2:h] |
[6d 1:h] [6d 1:h] [7d 2:h] [1 3:h] |
[7d 2:h]. ([6d 1:h]) [6d 1:h]^ |||
```

### 时值-连音

本节新增元素：
- `[notes:X]`：连音组，冒号后的数字表示整个组占用几份当前位置时值

连音组会先继承当前位置的基本时值。若外面包了一层括号，当前位置时值就减半。因此`{4/4} ([1 2 3:2])` 表示“三个音占两个八分音符”，也就是常见的一拍三连音。《月光奏鸣曲》中就有大量三连音：

```m3n
{key=E} {4/4} {120qpm}
([3d 6d 1:2]) ([3d 6d 1:2]) ([3d 6d 1:2]) ([3d 6d 1:2]) |
([3d 6d 1:2]) ([3d 6d 1:2]) ([3d 6d 1:2]) ([3d 6d 1:2]) |
([4d 6d 1:2]) ([4d 6d 1:2]) ([4d 7bd 2:2]) ([4d 7bd 2:2]) |
([3d 5#d 2:2]) ([3d 6d 1:2]) ([3d 6d 7d:2]) ([2d 5d 7d:2]) |||
```
组内音符均分整体时值，因此组内不允许出现时值相关元素。

## 构建乐曲结构

这一部分用于处理真实曲谱中的反复结构、多小节休止和换行等。学完之后，你可以把较长乐曲组织得更清楚。

### 处理反复

本节新增元素：
- `||:` ：前反复线
- `:||` ：后反复线
- `:||:` ：后反复结束后接新的前反复
- `:|||` ：后反复同时结束整首乐曲

核心形式是 `||: A段 :||`，意思是A段演奏完成后从前反复线再演奏一次。
如果只有后反复线，通常表示从乐曲开头再演奏一次。
可以通过播放这个简单的例子，理解反复的时间线：
```m3n
{key=C} {4/4}
1 2 3 4 | 5 6 7 1e :|| // 从头反复
1^^ | 1^^ |
||: 1e 7 6 5 | 4 3 2 1 :||| // 中间反复并结束
```
### 处理不同结尾

本节新增元素：
- `{volta=1} ... {/}`：第 1 房子
- `{volta=2,3} ... {/}`：第 2、3 房子
- `{volta=1,3~5} ... {/}`：第 1、3~5 房子

`volta` 这类需要关闭的属性，为了可读性也可以指定名称关闭 `{/volta}`。

当乐曲的最后一小段在每次演奏时有区别，用跳房子。因此《欢乐颂》的前两句可以合并为：

```m3n
{key=D} {4/4}
3 3 4 5 | 5 4 3 2 | 1 1 2 3 | {volta=1} 3. (2) 2^ {/}:|| {volta=2} 2. (1) 1^ {/}|||
```

### 标记段落顺序

本节新增元素：
- `{part=A} ... {/}`：正文中的段落标记
- `{parts=A B A C}`：文档开头的段落顺序说明
- `||` 双小节线（段落线）

较长歌曲可以先说明整体结构，再在正文中标出段落：

```m3n
{key=D} {4/4}
{parts=A B A C D C}

{part=A}
3 3 4 5 | 5 4 3 2 ||
{/}

{part=B}
1 1 2 3 | 3. (2) 2^ ||
{/}

{part=C}
1 1 2 3 | 2. (1) 1^ ||
{/}

{part=D}
2 2 3 1 | 2 (3 4) 3 1 | 2 (3 4) 3 2 | 1 2 5d 3~ | 3 3 4 5 | 5 4 3 2 ||
{/}
```

### 多小节休止

本节新增元素：
- `{rest=N}`：多小节休止

多小节休止适合跳过较长空白段：

```m3n
{key=C} {4/4}
1 2 3 4 | {rest=4} | 4 3 2 1 |||
```

### 控制谱面换行

本节新增元素：
- `{br}`：建议渲染器在这里换行

渲染器通常会自动排版。需要明确分句时使用：

```m3n
{title=小星星}
{key=C} {4/4}
1 1 5 5 | 6 6 5^ |{br}
4 4 3 3 | 2 2 1^ |||
```
这不会改变音乐内容，只影响排版效果。

## 丰富音乐细节

这一部分用于补充音乐中的表情、奏法、装饰音、和弦、歌词和低音。

### 添加演奏表情

本节新增元素：
- `{lg} ... {/}`：连奏区间
- `{p}`、`{mf}`、`{f}` 等：力度记号
- `{cresc} ... {/}`、`{decres} ... {/}`：渐强、渐弱区间

```m3n
{key=D} {4/4}
{p}{lg}3 3 4 5{/} | {mf}{lg}5 4 3 2{/} |
{cresc}1 1 2 3{/} | {f}3. (2) 2^ |||
```

### 添加奏法记号

本节新增元素：
- `{tr}`：颤音
- `{str}`：重音
- `{brk}`：顿音
- `{tip}`：跳音
- `{f1}` ~ `{f5}`：指法

> 更多奏法记号参见手册。

奏法记号也附着在前一个音符后面：

```m3n
{key=C} {4/4}
5{tr} 5{str} 5{brk} 5{tip} |
5{f1} 5{f2} 5{f3} 5{f4} |||
```

### 添加装饰音

本节新增元素：
- `{ac(...)}`：短倚音，来自 acciaccatura
- `{ap(...)}`：长倚音，来自 appoggiatura

装饰音附着在前一个主音后面。短倚音通常渲染为带斜线的小音符，主音时值不变；长倚音通常渲染为不带斜线的小音符，并占用主音时值。

```m3n
{key=C} {4/4}
3{ac(2)} 4 5{ac(34)} 3 |
5{ap(6)} 4 3{ap(2)} 1 |||
```

### 添加和弦标记

本节新增元素：
- `{chord=...}`：和弦标记

和弦标记从当前位置生效，持续到下一个和弦标记：

```m3n
{key=D} {4/4}
{chord=I}3 3 4 5 | {chord=V}5 4 3 2 |
{chord=I}1 1 2 3 | {chord=V}3. (2) 2^ |
{chord=I}3 3 4 5 | {chord=V}5 4 3 2 |
{chord=I}1 1 2 3 | {chord=V}2. (1) {chord=I}1^ |||
```

### 给旋律配歌词

本节新增元素：
- `{lyrics} ... {/}`：歌词块
- `%`：无歌词占位
- `-`：英文拆音节

歌词写在所有音乐正文结束后，作为一个独立补充片段添加。每个以空格分隔的词语对应一个有音高的音符，休止符不占歌词位置：

```m3n
{title=小星星}
{key=C} {4/4}
1 1 5 5 | 6 6 5^ |
4 4 3 3 | 2 2 1^ |||

{lyrics}
一 闪 一 闪 亮 晶 晶
满 天 都 是 小 星 星
{/}
```

英文歌词可以用连字符拆到多个音符：

```m3n
{key=C} {4/4}
1 1 5 5 | 6 6 5^ |||

{lyrics}
Twin-kle twin-kle lit-tle star
{/}
```

如果某个音没有歌词，用 `%` 占位：

```m3n
{key=C} {4/4}
1 1 5 5 | 6 6 5^ |||

{lyrics}
一 闪 % 闪 亮 晶 晶
{/}
```

如果反复段落在不同遍次有不同歌词，可以给歌词块标注适用遍次：

```m3n
{key=C} {4/4}
3 3 4 5 | 5 4 3 2 | 1 1 2 3 | 3. (2) 2^ :||

{lyrics=1}
欢 乐 女 神 圣 洁 美 丽
灿 烂 光 芒 照 大 地
{/}

{lyrics=2}
谁 能 做 个 忠 实 朋 友
献 出 高 贵 友 % 谊
{/}
```

更复杂的遍次范围写法可以在手册中查询。

### 添加低音谱表

本节新增元素：
- `{bass} ... {/}`：低音谱表块

低音谱表写在乐谱正文完成之后，作为独立补充片段。音高和时值写法与主旋律一致，但应只放谱表级内容，具体规则参考手册。

```m3n
{key=D} {4/4}
3 3 4 5 | 5 4 3 2 | 1 1 2 3 | 2. (1) 1^ |||

{bass}
1d^^ | 5d^^ | 1d^^ | 5d^ 1d^ |||
{/}
```
